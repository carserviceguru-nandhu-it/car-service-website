[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Add Proxy for API once Vercel is deployed
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-on-vercel.vercel.app/api/:splat"
#   status = 200
#   force = true
